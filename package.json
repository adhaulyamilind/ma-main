{
  "name": "gst-import-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run worker --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}

