version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
    volumes:
      - ./backend:/app
    command: npm run dev

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "5173:80"
